import LoginForm from "../components/LoginForm";

export default function LoginPage() {
  return (
    <main className="flex min-h-[calc(100vh-6rem)] w-full items-center justify-center px-4 py-12">
      <LoginForm />
    </main>
  );
}
